# dependencies
node_modules/

# build output
dist/
build/
*.tsbuildinfo

# env & secrets
.env
.env.*
!.env.example
**/*.env

# prisma
apps/api/prisma/*.db

# test / coverage
coverage/

# logs
*.log
npm-debug.log*

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# claude (local-only overrides; keep shared .claude/settings.json committed)
.claude/settings.local.json
CLAUDE.local.md

# Plenki/ is a SEPARATE nested git repo (design specs + live frontend).
# Read-only reference here; tracked in its own repo, not this backend repo.
Plenki/

# PlenkiFrontendFinal/ is the CURRENT frontend workspace — a separate nested
# git repo (clone of Dima's Plenki). Integration commits go there, not here.
# Decision: docs/superpowers/specs/2026-07-07-plenki-frontend-final-backend-alignment-design.md
PlenkiFrontendFinal/

# brainstorm visual-companion sessions
.superpowers/
.worktrees/

# scratch / generated assets
.playwright-mcp/
/release/

# gateway-agent local state (offline buffer, spooled labels)
.gateway-buffer/
